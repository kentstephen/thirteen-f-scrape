{"queries":[{"id":"quarters","compiledQueryString":"SELECT DISTINCT report_date\nFROM thirteenf.fund_summary\nORDER BY report_date DESC","inputQueryString":"SELECT DISTINCT report_date\nFROM thirteenf.fund_summary\nORDER BY report_date DESC","compiled":false,"inline":true},{"id":"fund_count","compiledQueryString":"SELECT COUNT(DISTINCT fund) as n\nFROM thirteenf.fund_summary","inputQueryString":"SELECT COUNT(DISTINCT fund) as n\nFROM thirteenf.fund_summary","compiled":false,"inline":true},{"id":"fund_aum","compiledQueryString":"SELECT\n    s.fund,\n    s.report_date,\n    s.total_value_m,\n    s.positions\nFROM thirteenf.fund_summary s\nWHERE s.report_date = (SELECT MAX(report_date) FROM thirteenf.fund_summary WHERE fund = s.fund)\nAND CAST(s.report_date AS VARCHAR) like '${inputs.quarter.value}'\nORDER BY s.total_value_m DESC\nLIMIT ${inputs.show_count.value}","inputQueryString":"SELECT\n    s.fund,\n    s.report_date,\n    s.total_value_m,\n    s.positions\nFROM thirteenf.fund_summary s\nWHERE s.report_date = (SELECT MAX(report_date) FROM thirteenf.fund_summary WHERE fund = s.fund)\nAND CAST(s.report_date AS VARCHAR) like '${inputs.quarter.value}'\nORDER BY s.total_value_m DESC\nLIMIT ${inputs.show_count.value}","compiled":false,"inline":true},{"id":"new_positions","compiledQueryString":"SELECT fund, issuer, current_value_m, current_shares\nFROM thirteenf.position_changes pc\nWHERE change_type = 'NEW'\nAND fund IN (SELECT fund FROM (SELECT\n    s.fund,\n    s.report_date,\n    s.total_value_m,\n    s.positions\nFROM thirteenf.fund_summary s\nWHERE s.report_date = (SELECT MAX(report_date) FROM thirteenf.fund_summary WHERE fund = s.fund)\nAND CAST(s.report_date AS VARCHAR) like '${inputs.quarter.value}'\nORDER BY s.total_value_m DESC\nLIMIT ${inputs.show_count.value}))\nORDER BY current_value_m DESC\nLIMIT 20","inputQueryString":"SELECT fund, issuer, current_value_m, current_shares\nFROM thirteenf.position_changes pc\nWHERE change_type = 'NEW'\nAND fund IN (SELECT fund FROM ${fund_aum})\nORDER BY current_value_m DESC\nLIMIT 20","compiled":true,"inline":true},{"id":"biggest_increases","compiledQueryString":"SELECT fund, issuer, pct_change, value_change_m, current_value_m, current_quarter, prior_quarter\nFROM thirteenf.position_changes\nWHERE change_type = 'INCREASED' AND pct_change IS NOT NULL\nAND fund IN (SELECT fund FROM (SELECT\n    s.fund,\n    s.report_date,\n    s.total_value_m,\n    s.positions\nFROM thirteenf.fund_summary s\nWHERE s.report_date = (SELECT MAX(report_date) FROM thirteenf.fund_summary WHERE fund = s.fund)\nAND CAST(s.report_date AS VARCHAR) like '${inputs.quarter.value}'\nORDER BY s.total_value_m DESC\nLIMIT ${inputs.show_count.value}))\nORDER BY value_change_m DESC\nLIMIT 20","inputQueryString":"SELECT fund, issuer, pct_change, value_change_m, current_value_m, current_quarter, prior_quarter\nFROM thirteenf.position_changes\nWHERE change_type = 'INCREASED' AND pct_change IS NOT NULL\nAND fund IN (SELECT fund FROM ${fund_aum})\nORDER BY value_change_m DESC\nLIMIT 20","compiled":true,"inline":true},{"id":"biggest_decreases","compiledQueryString":"SELECT fund, issuer, pct_change, value_change_m * -1 as value_sold_m, current_value_m, current_quarter, prior_quarter\nFROM thirteenf.position_changes\nWHERE change_type = 'DECREASED'\nAND fund IN (SELECT fund FROM (SELECT\n    s.fund,\n    s.report_date,\n    s.total_value_m,\n    s.positions\nFROM thirteenf.fund_summary s\nWHERE s.report_date = (SELECT MAX(report_date) FROM thirteenf.fund_summary WHERE fund = s.fund)\nAND CAST(s.report_date AS VARCHAR) like '${inputs.quarter.value}'\nORDER BY s.total_value_m DESC\nLIMIT ${inputs.show_count.value}))\nORDER BY value_change_m ASC\nLIMIT 20","inputQueryString":"SELECT fund, issuer, pct_change, value_change_m * -1 as value_sold_m, current_value_m, current_quarter, prior_quarter\nFROM thirteenf.position_changes\nWHERE change_type = 'DECREASED'\nAND fund IN (SELECT fund FROM ${fund_aum})\nORDER BY value_change_m ASC\nLIMIT 20","compiled":true,"inline":true},{"id":"popular","compiledQueryString":"SELECT issuer, num_funds, total_value_m\nFROM thirteenf.top_stocks\nORDER BY num_funds DESC, total_value_m DESC\nLIMIT 25","inputQueryString":"SELECT issuer, num_funds, total_value_m\nFROM thirteenf.top_stocks\nORDER BY num_funds DESC, total_value_m DESC\nLIMIT 25","compiled":false,"inline":true}]}